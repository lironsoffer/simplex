\documentclass{article}

\usepackage[final,nonatbib]{neurips_2024}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{enumitem}
\usepackage{placeins}

\hypersetup{colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue}

% Remove NeurIPS conference footer
\makeatletter
\renewcommand{\@noticestring}{}
\makeatother

\title{Belief State Geometry in Non-Ergodic Transformers:\\
A Mess3 Mixture Experiment}
\author{}
\date{March 2026}

\begin{document}
\maketitle

\begin{abstract}
We train a small transformer on a non-ergodic mixture of three Mess3 Hidden Markov
Model processes, where each training sequence is generated by one component.
This construction requires the model to solve two epistemically distinct tasks:
identifying which component is active (meta-belief $\pi$) and tracking
within-component state ($\eta_k$).
We pre-register the prediction that residual stream activations encode these in
orthogonal subspaces (H2: the hypothesis that $\pi$ and each $\eta_k$ are encoded in mutually orthogonal residual stream subspaces).
Analysis confirms H2 for the $\pi$--$\eta$ split (removing the $\pi$ subspace
causes $<$0.001 drop in $\eta_k$ regression $R^2$), but not for the
$\eta_k$--$\eta_{k'}$ structure (overlap 0.53--0.69), consistent with superposition.
$R^2(\pi)$ rises sharply to near 1.0 by position~2, well before the
synchronisation horizon $N^* \approx 9.6$ (Riechers et al., 2025):
high linear decodability requires only systematic covariation with component
identity across sequences, not full convergence of $\pi$ to a vertex.
Training dynamics reveal $\eta_k$ representations emerge before $\pi$
(step~100 vs.\ 2{,}000).
A dimension sweep shows the model prioritises $\eta_k$ over $\pi$ under capacity
pressure.
\end{abstract}

\tableofcontents
\newpage

% ──────────────────────────────────────────────────────────────────────────────
\section{Motivation}
\label{sec:motivation}
% ──────────────────────────────────────────────────────────────────────────────

Real language corpora are \emph{non-ergodic}: each document belongs to one
domain or style and never switches mid-document.
A model trained on such data must solve two epistemically distinct tasks:
identify \emph{which} type of document it is reading (meta-belief $\pi$),
and track fine-grained structure within that type (within-component belief $\eta_k$).
We formalise this with a non-ergodic mixture of $K=3$ Mess3 HMMs, a clean,
analytically tractable proxy where ground-truth belief states are computable
exactly.

The experimental design is grounded in three prior results:
\begin{itemize}[noitemsep]
  \item Shai et al.~\cite{shai2024belief} show that a transformer trained on a
        single ergodic Mess3 process encodes belief states as a fractal in the
        residual stream.
  \item Riechers et al.~\cite{riechers2025} prove that non-ergodic pretraining
        necessarily produces ICL, governed by a synchronisation horizon $N^*$.
  \item Shai et al.~\cite{shai2025factored} show that parallel factors are
        encoded in orthogonal subspaces, a result for simultaneous streams,
        not temporal mixtures.
\end{itemize}
This experiment combines the non-ergodic setup of Riechers et al.\ with the
geometry analysis of Shai et al.\ to ask: \emph{non-ergodic mixture $\to$
factored geometry?}
It also provides a geometric complement to Riechers et al.: their
synchronisation horizon $N^*$ predicts \emph{when} the model commits to one
component; we ask \emph{where} in the residual stream that commitment
is encoded.

% ──────────────────────────────────────────────────────────────────────────────
\section{Setup}
\label{sec:setup}
% ──────────────────────────────────────────────────────────────────────────────

\subsection{Data}

\textbf{Mess3 HMM.}
A Mess3 process is a 3-state, 3-token edge-emitting Hidden Markov Model
parameterised by emission strength $\alpha \in (0.5, 1)$ and mixing rate
$x \in (0, 1/3)$.
The \emph{token-labeled transition matrix} $T^{(a)} \in \mathbb{R}^{3 \times 3}$
encodes the joint probability of emitting token $a$ and transitioning to the
next state:
\[
T^{(a)}_{ij} = P(\text{token}=a \mid s_t=i)\cdot P(s_{t+1}=j \mid s_t=i),
\]
where $P(\text{token}=a \mid s=i) = \alpha$ if $a=i$, else $(1-\alpha)/2$,
and $P(s'=j \mid s=i) = 1-2x$ if $i=j$, else $x$.
Sequences are generated by starting in a state drawn from the (uniform)
stationary distribution, then at each step emitting a token according to the
current state and transitioning to the next state jointly.

\textbf{Components.} We use three Mess3 HMMs with the following parameters (Table~\ref{tab:components}):

\begin{table}[h]
\centering
\begin{tabular}{lccl}
\toprule
Component & $\alpha$ & $x$ & Character \\
\midrule
A & 0.95 & 0.02 & Sharp fractal, slow mixing \\
B & 0.80 & 0.08 & Medium fractal, moderate mixing \\
C & 0.55 & 0.25 & Sparse attractor, fastest mixing \\
\bottomrule
\end{tabular}
\caption{Mess3 component parameters. $\alpha$ is emission strength;
$x$ is the off-diagonal transition probability.}
\label{tab:components}
\end{table}

Parameters were chosen so that: (a) components are distinguishable within 16
tokens ($N^* \approx 9.6 < 16$), and (b) each component produces a distinct
fractal geometry in belief space.
Figure~\ref{fig:msp_fractals} shows the Mixed State Presentation (MSP) attractor
for each component: the fractal attractor of the belief state iterated function
system (IFS).

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../figures/msp_fractals.png}
\caption{Ground-truth MSP fractals for each Mess3 component, simulated by
iterating the IFS $f_a(\eta) = \eta T^{(a)} / (\eta T^{(a)} \mathbf{1})$ for
80{,}000 steps. Component A (blue) produces a rich self-similar fractal;
component C (green) has only $\sim$9 reachable belief states due to weak
emission ($\alpha=0.55$).
These three geometries define the expected residual stream structure under H2.}
\label{fig:msp_fractals}
\end{figure}

\textbf{Sequence generation.} To form the training corpus, a component
$k \sim \mathrm{Uniform}(\{A, B, C\})$ is sampled for each sequence.
A token sequence $x_{1:L}$ of length $L=16$ (the context window) is then
generated from component $k$ alone, starting from the stationary distribution.
The context window was set to $L=16$ to satisfy $N^* < L$: with
$N^* \approx 9.6$, the model accumulates enough context to synchronise to the
correct component within a single sequence.
The corpus is \emph{non-ergodic} because sequences never switch components;
each sequence is a pure draw from one Mess3 process.
Figure~\ref{fig:seq_example} shows one example sequence per component; the
visual contrast in token diversity across rows reflects the very different
$\alpha$ and $x$ values.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../figures/sequence_example_2d.png}
\caption{One example sequence (length 16) from each component.
Cell color encodes token value (blue\,=\,0, orange\,=\,1, green\,=\,2).
Component~A ($\alpha=0.95$) is nearly always token~0; component~C
($\alpha=0.55$) mixes all three tokens freely.
The dashed line marks the synchronisation horizon $N^* \approx 9.6$:
before this point the identity of the component is ambiguous; after it
a Bayesian observer has committed.
The contrast in token diversity across rows reflects the components' different
emission strengths and makes them distinguishable within $N^*$ tokens.}
\label{fig:seq_example}
\end{figure}

\textbf{Ground-truth belief states.}
At each position $t$, the Bayesian-optimal agent maintains a joint belief
$(\pi(t), \eta_A(t), \eta_B(t), \eta_C(t))$, where $\pi(t) \in \Delta^2$ is
the meta-belief over components and $\eta_k(t) \in \Delta^2$ is the
within-component belief for component $k$.
Both are initialised to the uniform stationary distribution (which equals
uniform for Mess3 by symmetry) and updated after each observed token using
the two-step rule (Section~\ref{sec:math}).
These analytically computed belief states are stored alongside each sequence
and used as regression targets in the geometry analysis.
50{,}000 training sequences and 5{,}000 validation sequences were generated.

Figure~\ref{fig:joint} illustrates the full joint 9-state belief geometry:
the meta-belief $\pi$ lives on the central simplex, while each vertex hosts
the MSP fractal of the corresponding component.
The three fractals are disconnected, reflecting the non-ergodic block structure.

\begin{figure}[h]
\centering
\includegraphics[width=0.6\textwidth]{../figures/joint_9state_attractor.png}
\caption{Joint 9-state belief geometry.
The centre shows the meta-belief $\pi$ simplex (which component?);
each vertex hosts the MSP fractal attractor of that component (where within?).
The three fractals are disconnected (no probability mass flows between
components), reflecting the non-ergodic block structure of the corpus.}
\label{fig:joint}
\end{figure}

\subsection{Architecture}

We use a small GPT-2-style transformer implemented via TransformerLens~\cite{nanda2022tl}:
2~layers, $d_\mathrm{model}=64$, 4~heads, $d_\mathrm{head}=16$,
$d_\mathrm{mlp}=256$, context window 16, vocabulary $\{0,1,2\}$, $\approx$101k
parameters.

\subsection{Training}

Cross-entropy next-token prediction, AdamW ($\mathrm{lr}=10^{-3}$, cosine
decay to $10^{-5}$, 1{,}000 warmup steps), 50{,}000 steps total.
Batch size 64 (3{,}200{,}000 total sequence exposures over 50{,}000 steps).
Sequences are generated on-the-fly each step to prevent overfitting.
Checkpoints saved at steps 100, 500, 1{,}000, 2{,}000, then every 5{,}000
steps; at each checkpoint we run regression at the final layer and final
position to track how $R^2$ evolves over training.

% ──────────────────────────────────────────────────────────────────────────────
\section{Hypotheses and Predictions}
\label{sec:predictions}
% ──────────────────────────────────────────────────────────────────────────────

\subsection{Possible Hypotheses}

\begin{description}[leftmargin=2em]
\item[H1 (Joint):] The transformer represents $\eta_\mathrm{joint}$ as one
  undifferentiated 8D blob. High $R^2$ for all targets, but projecting out
  the $\pi$ subspace substantially degrades $\eta_k$ regression.
\item[H2 (Factored):] Four orthogonal subspaces: one 2D $\pi$ subspace and
  three 2D $\eta_k$ subspaces. Projecting out $\pi$ leaves $\eta_k$
  $R^2 \approx$ unchanged.
\item[H3 (Superposition):] $\pi$ and $\eta_k$ recoverable approximately but
  packed into fewer than 8 effective dimensions via near-orthogonal
  superposition.
\end{description}

\subsection{Intuitive Prediction}

Our geometric intuition is that the residual stream should mirror the
ground-truth joint belief geometry: the \emph{nested fractal} shown in
Figure~\ref{fig:joint}, with $\pi$ occupying a central simplex and each vertex
of that simplex hosting the MSP fractal attractor of the corresponding
component.

We favour H2 (Factored) by analogy to Shai et al.~\cite{shai2025factored},
where the setting is a \emph{parallel} joint process: at each timestep
independent subprocesses each emit a token and the observation is their
concatenation.
Our setup is \emph{sequential} (each sequence comes from one subprocess), but
the underlying math is the same: block-diagonal transition matrices,
conditionally independent factors, and a belief space that decomposes into
orthogonal subspaces.
If transformers learn factored representations in the parallel case, we expect
them to do so here too.
We therefore pre-register H2: $\pi$ and each $\eta_k$ are encoded in mutually
orthogonal residual stream subspaces, with CEV dimensionality $\approx 8$
(four 2D subspaces: one for $\pi$, one each for $\eta_A$, $\eta_B$, $\eta_C$).

\subsection{Mathematical Derivation}
\label{sec:math}

\textbf{The joint belief state.}
The joint hidden state space is $\mathcal{S}_\mathrm{joint} = \mathcal{S}_A
\sqcup \mathcal{S}_B \sqcup \mathcal{S}_C$ (9 states).
The joint token-labeled transition matrices are block-diagonal:
\[
T^{(x)}_\mathrm{joint} = \mathrm{diag}\!\left(T_A^{(x)},\, T_B^{(x)},\,
T_C^{(x)}\right).
\]
The zeros off the block diagonal encode non-ergodicity exactly: no probability
mass can flow between components.
This block structure means the joint belief state $\eta_\mathrm{joint} \in
\Delta^8$ (the 9-state probability simplex, affine dimension~8) decomposes uniquely as
\[
\eta_\mathrm{joint}[k, s] = \pi_k \cdot \eta_k[s],
\]
where $\pi \in \Delta^2$ is the \emph{meta-belief} and $\eta_k \in \Delta^2$
is the \emph{within-component belief}.

\textbf{The two-step update.}
Upon observing token $x$:
\begin{align}
L_k(x) &= \eta_k \cdot T_k^{(x)} \cdot \mathbf{1}
          \quad \text{(per-component likelihood)}, \label{eq:likelihood}\\
\pi'_k &= \frac{\pi_k \cdot L_k(x)}{\sum_{j} \pi_j L_j(x)}
          \quad \text{(meta-belief Bayes update)}, \label{eq:pi-update}\\
\eta'_k &= \frac{\eta_k \cdot T_k^{(x)}}{L_k(x)}
           \quad \text{(within-component Bayes update)}. \label{eq:eta-update}
\end{align}
Crucially, equation~\eqref{eq:eta-update} applies the standard single-Mess3
update \emph{independently} to each component.
The components couple only through the normalisation constant in
\eqref{eq:pi-update}.

\textbf{Synchronisation horizon.}
The \emph{synchronisation horizon} $N^*$ is the characteristic number of
tokens required for the meta-belief $\pi(t)$ to collapse near the vertex of
the true component, given by
\[
N^* = \frac{1}{\min_{k \neq k'} D_\mathrm{KL}(P_k \| P_{k'})},
\]
where $D_\mathrm{KL}$ is estimated empirically via per-token log-likelihood
ratios over long sequences.
With the chosen parameters, $N^* \approx 9.6 < 16$, so synchronisation
completes within every training sequence.

% ──────────────────────────────────────────────────────────────────────────────
\section{Methods}
\label{sec:methods}
% ──────────────────────────────────────────────────────────────────────────────

\textbf{PCA and CEV dimensionality.}
To measure how many dimensions the transformer uses to represent the belief state,
we run PCA on the matrix of all residual stream vectors per layer, flattening
sequences and positions into rows to obtain an $(N \cdot L) \times d_\mathrm{model}$
matrix.
The principal components are ordered by variance explained.
The \textbf{CEV (cumulative explained variance) dimensionality} at threshold $p$ is
the smallest $d$ such that the top-$d$ components together explain $\geq p\%$ of
total variance: the minimum number of directions needed to capture $p\%$ of the
structure in the activations.

\textbf{Regression-based subspace identification}~\cite{shai2024belief}.
We fit a linear map $W \in \mathbb{R}^{3 \times d_\mathrm{model}}$ from residual
stream activations $\mathbf{a} \in \mathbb{R}^{d_\mathrm{model}}$ (normalised to
zero mean and unit variance per dimension) to ground-truth belief targets
$\mathbf{b} \in \mathbb{R}^3$ (either $\pi$ or $\eta_k$) via Ridge regression
($\lambda = 10^{-4}$), and measure linear decodability by $R^2$ on a held-out
20\% test split (fit on 80\%).
To identify \emph{where} in the residual stream the belief is encoded, we decompose $W = U\Sigma V^\top$, where the columns of $V$ are orthonormal directions
in activation space ordered by their contribution to the prediction.
Since belief states live on a 2-simplex (affine dimension 2), we take the top-2
columns of $V$ as the belief-encoding subspace.
This basis is used in all subsequent overlap tests.

\textbf{Subspace overlap metric}~\cite{shai2025factored}.
Given two subspaces with orthonormal bases $Q_A \in \mathbb{R}^{k_A \times d}$ and
$Q_B \in \mathbb{R}^{k_B \times d}$ (orthonormal rows), the overlap is
\[
  \mathrm{overlap}(Q_A, Q_B) = \frac{\|Q_A Q_B^\top\|_F}{\sqrt{k_A \cdot k_B}}.
\]
The $(i,j)$ entry of $Q_A Q_B^\top$ is the cosine $\cos\theta_{ij}$ between the
$i$-th basis vector of $Q_A$ and the $j$-th basis vector of $Q_B$; the Frobenius
norm is the square root of the sum of their squares, and dividing by $\sqrt{k_A k_B}$ normalises to $[0, 1]$
(0 = orthogonal subspaces, 1 = identical subspaces).

\textbf{Fractal visualisation.}
We visualise whether the residual stream encodes the full fractal geometry of
each component's belief attractor, not merely a summary statistic.
For each component $k$, we take residual stream activations from component-$k$
sequences at positions $t > N^*$ (after the meta-belief $\pi$ has collapsed,
so activations reflect pure component-$k$ belief states), apply the $\eta_k$
regression map to obtain predicted $\hat\eta_k \in \mathbb{R}^3$, and plot the
resulting point cloud in barycentric simplex coordinates.
We compare this cloud against the ground-truth MSP attractor, generated by
iterating the IFS $f_a(\eta) = \eta T^{(a)} / (\eta T^{(a)}\mathbf{1})$ for
80{,}000 steps.


% ──────────────────────────────────────────────────────────────────────────────
\section{Results}
\label{sec:results}
% ──────────────────────────────────────────────────────────────────────────────

\subsection{PCA Dimensionality}

\begin{table}[h]
\centering
\begin{tabular}{lcccc}
\toprule
Layer & CEV (95\%) & CEV (98\%) & CEV (100\%) & Predicted \\
\midrule
0 & 6 & 9  & 64 & 8 \\
1 & 5 & 7  & 64 & 8 \\
\bottomrule
\end{tabular}
\caption{PCA dimensionality per layer. CEV($p$\%) = fewest components explaining $\geq p\%$ of variance.
The first 5--6 dims capture 95\%, 7--9 capture 98\%, and all 64 carry non-zero variance,
revealing a low-dimensional dominant subspace atop a long noise tail.}
\label{tab:pca}
\end{table}

Table~\ref{tab:pca} reveals a striking compression: the transformer uses fewer dimensions
than the theoretical belief space requires.
Theory predicts 8 dimensions (four 2D subspaces), yet 98\% of variance is
captured in just 7 dimensions at the final layer, and 95\% in only 5.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{../figures/pca_explained_variance.png}
\caption{Cumulative explained variance (CEV) by context position, for Layer~0 (dashed)
and Layer~1 (solid). The predicted 8 dimensions under H2 are marked by the
dotted vertical line. Layer~1 explains 95\% of variance in just 5 components,
more compressed than the theoretical minimum, consistent with fractal sparsity
and superposition.}
\label{fig:pca}
\end{figure}
  
Figure~\ref{fig:pca} plots this.
This suggests the transformer is \emph{more efficient} than the theoretical
minimum representation.
We offer three possible explanations, though we cannot confirm which dominates:
\begin{enumerate}[noitemsep]
  \item \textbf{Component C's near-degenerate attractor.}
        With $\alpha=0.55$, component C has only $\sim$9 reachable belief
        states, collapsing $\eta_C$ from a 2D subspace toward a near-discrete
        structure that requires far less than 2 dimensions.
  \item \textbf{Fractal attractors don't fill their 2D subspaces uniformly.}
        Each MSP attractor is a fractal with dimension strictly less than 2.
        Belief states cluster along lower-dimensional structures within each
        2D subspace, so PCA sees less than 2 effective dimensions per factor.
  \item \textbf{Superposition enabled by attractor sparsity.}
        Because the attractors are sparse, the transformer can pack multiple
        belief directions into shared dimensions without interference, achieving
        near-perfect $R^2 > 0.95$ with fewer dimensions than a uniform
        distribution would require.
\end{enumerate}
At 100\% CEV both layers require all 64 dimensions,
confirming a sharp transition between the dominant low-dimensional belief
subspace and a long tail of noise dimensions.


\subsection{Linear Decodability}

\begin{table}[h]
\centering
\begin{tabular}{lc}
\toprule
Quantity & $R^2$ \\
\midrule
$\pi$ (meta-belief)   & 0.957 \\
$\eta_A$ (component A) & 0.950 \\
$\eta_B$ (component B) & 0.963 \\
$\eta_C$ (component C) & 0.984 \\
\bottomrule
\end{tabular}
\caption{Ridge regression $R^2$ at the final layer, final context position
(position 15).}
\label{tab:r2}
\end{table}

All belief targets are linearly decodable with $R^2 > 0.95$~\cite{li2023othello},
confirming that the residual stream encodes the full joint belief state.

Figure~\ref{fig:r2_pos} shows $R^2$ as a function of context position.
$R^2(\eta_k)$ is near 1.0 from position~1, confirming that within-component
belief is encoded reliably from the very first token.
$R^2(\pi)$ starts at 0 (no context at position~1) and rises sharply to
near 1.0 by position~2, remaining stable thereafter.
The aggregate $R^2(\pi) = 0.956$ in Table~\ref{tab:r2} reflects this
stability averaged over all positions.

This early rise indicates the model has learned the emission statistics of
each component during training.
The first token is already highly diagnostic: token~0 is emitted with
probability $\alpha_A = 0.95$ under component~A but only $\approx 0.3$ under
component~C, so the Bayesian posterior over components shifts substantially
after a single observation.
The model encodes this partial posterior accurately and immediately.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{../figures/r2_vs_position.png}
\caption{$R^2$ vs.\ context position for Layer~1.
$R^2(\eta_k)$ (coloured) is near 1.0 from position~1.
$R^2(\pi)$ (black) starts at 0 and rises sharply to near 1.0 by position~2.}
\label{fig:r2_pos}
\end{figure}

\subsection{Subspace Orthogonality}

\begin{table}[h]
\centering
\begin{tabular}{lcccc}
\toprule
 & $\pi$ & $\eta_A$ & $\eta_B$ & $\eta_C$ \\
\midrule
$\pi$     & 1.000 & 0.193 & 0.171 & 0.176 \\
$\eta_A$  & 0.193 & 1.000 & 0.691 & 0.534 \\
$\eta_B$  & 0.171 & 0.691 & 1.000 & 0.590 \\
$\eta_C$  & 0.176 & 0.534 & 0.590 & 1.000 \\
\bottomrule
\end{tabular}
\caption{Subspace overlap matrix ($0=$ orthogonal, $1=$ identical).
$\pi$ is nearly orthogonal to all $\eta_k$ subspaces (0.17--0.19).
The $\eta_k$ subspaces overlap each other moderately, reflecting the shared
token alphabet.}
\label{tab:overlap}
\end{table}

$\pi$ is nearly orthogonal to all $\eta_k$ subspaces (overlap 0.17--0.19).
The moderate $\eta_k$--$\eta_{k'}$ overlap (0.53--0.69) reflects the shared
3-token alphabet: all components respond to the same tokens, inducing partially
shared within-component directions, consistent with superposition~\cite{elhage2022superposition}.

As a direct functional test, removing the $\pi$ subspace from activations and
re-running $\eta_k$ regression yields $R^2$ drops of less than 0.001 in all cases,
and removing any $\eta_k$ subspace leaves $\pi$ regression unchanged, confirming
that the subspaces are functionally independent, not merely geometrically
near-orthogonal.

\subsection{Fractal Geometry}

Figure~\ref{fig:fractal} shows, for each component, the ground-truth belief
state trajectories (left) and the predicted $\eta_k$ obtained by applying the
regression weights to residual stream activations (right), both plotted in
barycentric simplex coordinates.

The regression output faithfully reproduces the fractal structure of each
component's MSP attractor, confirming that the model encodes the full geometric
structure of the belief distribution, not merely its mean.
Component~A exhibits a rich self-similar fractal; component~B a moderately
nested fractal; component~C a sparse set of $\sim$9 discrete clusters.

\begin{figure}[h!]
\centering
\includegraphics[width=0.72\textwidth]{../figures/fractal_comparison.png}
\caption{Fractal comparison for each component: ground-truth $\eta_k$
trajectories (left) vs.\ regression-predicted $\eta_k$ from residual stream
activations (right), plotted in barycentric simplex coordinates.
The regression output reproduces the MSP fractal geometry.}
\label{fig:fractal}
\end{figure}

\subsection{Training Dynamics}

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{../figures/training_dynamics.png}
\caption{$R^2$ of $\pi$ and $\eta_k$ regressions over training steps
(final layer, final position). $\eta_k$ representations emerge early
(step~100, $R^2 > 0.9$); $\pi$ representation emerges later (step~2{,}000).
The ordering reflects loss-gradient efficiency: per-component prediction
reduces loss immediately, while distinguishing components requires
cross-component comparison~\cite{saxe2019semantic}.}
\label{fig:dynamics}
\end{figure}

Figure~\ref{fig:dynamics} shows that $\eta_k$
subspaces emerge \emph{before} the $\pi$ subspace.

The ordering (within-component beliefs first, meta-belief later) suggests
that the transformer first learns to track the local token statistics of each
component individually (which requires only learning the Mess3 update within
each component's context), and only subsequently learns to \emph{distinguish}
which component is active (which requires comparing cross-component
likelihoods).
This ordering makes sense: per-component prediction reduces loss immediately,
while improving $\pi$ only helps once the model has already learned the
within-component structure to compare.

% ──────────────────────────────────────────────────────────────────────────────
%\section{Additional Analysis: Vary-One}
%\label{sec:vary_one}
%
%\textbf{Motivation.}
%The vary-one analysis tests whether the learned $\eta_k$ subspaces are
%\emph{component-specific}.
%Under H2, each $\eta_k$ should live in its own 2D subspace.
%Fitting the $\eta_k$ regression on only component-$k$ sequences (fixing
%component identity, varying within-component positions) reveals whether the
%model has learned three distinct fractal geometries or one shared one.
%If the cross-component subspace overlap is low, each component occupies a
%distinct region of residual stream space.
%
%\textbf{Results.}
%
%\begin{table}[h]
%\centering
%\begin{tabular}{lc}
%\toprule
%Component & Within-component $\eta_k$ $R^2$ \\
%\midrule
%A & 0.990 \\
%B & 0.962 \\
%C & 0.980 \\
%\bottomrule
%\end{tabular}
%\caption{Vary-one $R^2$: regression fit using only sequences from component
%$k$.}
%\label{tab:vary_one}
%\end{table}
%
%Within-component $R^2$ is high for all components ($\geq 0.96$,
%Table~\ref{tab:vary_one}), confirming that sequences from the same component
%share a coherent subspace structure.
%For component~A the single-component fit is notably better (0.990 vs.\ 0.950),
%while for B and C the difference is small, likely because their more uniform
%attractors are already well-characterised by the global fit.
%
%\begin{table}[h]
%\centering
%\begin{tabular}{lccc}
%\toprule
% & A & B & C \\
%\midrule
%A & 1.000 & 0.275 & 0.208 \\
%B & 0.275 & 1.000 & 0.510 \\
%C & 0.208 & 0.510 & 1.000 \\
%\bottomrule
%\end{tabular}
%\caption{Cross-component subspace overlap from vary-one analysis.
%A and C are nearly orthogonal ($0.208$); A--B and B--C show moderate overlap,
%likely due to similar emission statistics.}
%\label{tab:vary_cross}
%\end{table}
%
%The cross-component overlaps (Table~\ref{tab:vary_cross}) are substantially
%lower than the $\eta_k$--$\eta_{k'}$ overlaps in the global analysis
%(Table~\ref{tab:overlap}), especially for A--C (0.208).
%This confirms that the transformer has learned \emph{component-specific}
%fractal geometries.
%Component B's moderate overlap with both A and C likely reflects its
%intermediate emission strength ($\alpha = 0.80$), making its belief geometry
%somewhat ``between'' those of A and C.

% ──────────────────────────────────────────────────────────────────────────────
\section{Additional Analysis: Dimension Sweep}
\label{sec:dim_sweep}
% ──────────────────────────────────────────────────────────────────────────────

\textbf{Motivation.}
The main experiment uses $d_\mathrm{model}=64$, giving the model roughly $8\times$
more residual stream dimensions than the theoretical minimum for a full factored
representation ($K+1$ subspaces of 2D each $= 8$ dimensions for $K=3$).
This raises the question: is the inductive bias toward factored representations
an artifact of surplus capacity, or does it persist under pressure?

Separately, we ask which belief state the model prioritises when it \emph{cannot}
represent both.
The factored representation result of Shai et al.~\cite{shai2025factored} says
$\pi$ and $\eta_k$ are equally important in the sense of conditional independence,
but they are not equally important for the \emph{next-token prediction task}.
Knowing $\pi$ alone does not determine the next-token distribution; one also needs
$\eta_k$ for the active component.
Conversely, $\eta_k$ (plus implicit knowledge of which component is active from
context) is sufficient.
After position $N^*$, the context itself encodes component identity, making $\pi$
in the residual stream redundant; $\eta_k$ must be maintained throughout.

We therefore expect that under capacity pressure the model encodes $\eta_k$ first.

\textbf{Setup.}
We train four additional models with $d_\mathrm{model} \in \{2, 4, 6, 8\}$,
keeping all other hyperparameters fixed (2 layers, $n_\text{heads} \times d_\text{head}
= d_\text{model}$ throughout, see Table~\ref{tab:dim_configs}).
Each model is trained for 20{,}000 steps on the pre-saved 50{,}000 training sequences
via a DataLoader (shuffled, cycled), which avoids the per-step HMM generation
overhead that dominates for tiny models.
Note that the main $d_\mathrm{model}=64$ model was trained on sequences generated
on-the-fly (50{,}000 steps); the sweep models use the pre-saved 50{,}000 training
sequences via a \texttt{DataLoader} (20{,}000 steps). Val losses are comparable
across all models, confirming convergence.
Note $d=8$ is the theoretical minimum capacity for a full factored representation.

\begin{table}[h]
\centering
\begin{tabular}{rrrrrr}
\toprule
$d_\mathrm{model}$ & $n_\mathrm{heads}$ & $d_\mathrm{head}$ & $d_\mathrm{mlp}$
  & Params & Val loss \\
\midrule
2  & 1 & 2 & 8   &     199 & 1.087 \\
4  & 2 & 2 & 16  &     587 & 0.880 \\
6  & 2 & 3 & 24  &   1{,}167 & 0.862 \\
8  & 4 & 2 & 32  &   1{,}939 & 0.863 \\
64 & 4 & 16 & 256 & 101{,}507 & 0.862 \\
\bottomrule
\end{tabular}
\caption{Model configurations for the dimension sweep.
Val loss at $d=6$ already matches $d=64$; the task is saturated at 6 dimensions.}
\label{tab:dim_configs}
\end{table}

\textbf{Results.}

\begin{table}[h]
\centering
\begin{tabular}{rrrrrr}
\toprule
$d$ & $R^2(\pi)$ & $R^2(\eta_A)$ & $R^2(\eta_B)$ & $R^2(\eta_C)$ & CEV dim \\
\midrule
2  & $-0.003$ & 0.384 & 0.387 & 0.449 & 2 \\
4  &  0.148   & 0.795 & 0.820 & 0.851 & 3 \\
6  &  0.649   & 0.771 & 0.799 & 0.745 & 4 \\
8  &  0.686   & 0.830 & 0.853 & 0.865 & 4 \\
64 &  0.957   & 0.950 & 0.963 & 0.984 & 5 \\
\bottomrule
\end{tabular}
\caption{Held-out $R^2$ for all belief state targets and PCA CEV
dimensionality, at the final layer and final context position.}
\label{tab:dim_r2}
\end{table}

Figure~\ref{fig:r2_vs_dim} and Table~\ref{tab:dim_r2} tell a clear story.
$\eta_k$ encoding saturates early: $R^2(\eta_k) \geq 0.80$ already at $d=4$,
and improves only marginally thereafter.
By contrast, $R^2(\pi)$ starts near zero and grows slowly, reaching only 0.69
at $d=8$ (the theoretical minimum for a full factored representation), and
0.957 only at $d=64$.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{../figures/dim_sweep/r2_vs_dim.png}
\caption{Held-out $R^2$ vs.\ $d_\mathrm{model}$ for all belief state targets.
$\eta_k$ (dashed) saturates by $d=4$; $\pi$ (solid black) grows slowly and requires
$d \gg 8$ to approach the quality achieved for $\eta_k$.
The dotted line marks $d=8$, the theoretical minimum for a full factored
representation.}
\label{fig:r2_vs_dim}
\end{figure}

The scatter plots in Figure~\ref{fig:scatter_d4_d6} confirm this geometrically.
At $d=4$, PC projections coloured by component identity (left) show a single
indistinguishable blob (the model has essentially zero $\pi$ encoding), while
the same projections coloured by dominant within-component state (right) form
clean diagonal bands.
At $d=6$, the first signs of component separation appear, but the $\eta_k$ dip
noted above is also visible: within-component structure is slightly degraded
relative to $d=4$, before recovering at $d=8$.

\begin{figure}[h]
\centering
\begin{subfigure}{\textwidth}
  \includegraphics[width=\textwidth]{../figures/dim_sweep/scatter_d4.png}
  \caption{$d=4$: no component separation (left); clear within-component bands
  (right).}
\end{subfigure}
\vspace{0.5em}
\begin{subfigure}{\textwidth}
  \includegraphics[width=\textwidth]{../figures/dim_sweep/scatter_d6.png}
  \caption{$d=6$: first sign of $\pi$ encoding, with slight $\eta_k$ dip (left);
  within-component structure preserved (right).}
\end{subfigure}
\caption{Residual stream PCA projections coloured by component identity (left)
and dominant within-component state (right).}
\label{fig:scatter_d4_d6}
\end{figure}

We note that at $d=6$, $R^2(\eta_k)$ \emph{drops} slightly relative to $d=4$
(e.g.\ $\eta_A$: $0.795 \to 0.771$), recovering at $d=8$.
This suggests that when the model first gains enough capacity to start encoding
$\pi$ ($d=6$), it does so partly at the expense of within-component encoding,
before recovering both at $d=8$.

\textbf{Interpretation.}
The result confirms the prediction: $\eta_k$ is prioritised over $\pi$ under
capacity pressure.
The explanation is information-theoretic.
The residual stream should encode what the context does not already make obvious.
$\pi$ collapses after $N^* \approx 9.6$ tokens; the context sequence implicitly
carries the component identity after that point.
$\eta_k$, by contrast, requires ongoing Bayesian integration and never becomes
obvious from the raw tokens; it must be maintained explicitly in the residual
stream throughout the sequence.
Furthermore, $\eta_k$ directly determines the next-token distribution
($P(\text{next}=a \mid \eta_k) = \eta_k \cdot T_k^{(a)} \cdot \mathbf{1}$ for each token $a$, after
synchronisation), while $\pi$ is a routing variable whose gradient signal vanishes
once synchronisation is complete.
At small $d$, the loss gradient therefore drives the model to encode $\eta_k$
first, leaving $\pi$ as a low-priority secondary representation.

Together, the four analyses above establish a consistent picture of the residual stream geometry, which we now interpret against the competing hypotheses.

% ──────────────────────────────────────────────────────────────────────────────
\section{Discussion}
\label{sec:discussion}
% ──────────────────────────────────────────────────────────────────────────────

\subsection{Which Hypothesis Best Fits?}

The picture is mixed: H2 holds cleanly for the $\pi$--$\eta$ split but not
for the internal structure of the $\eta_k$ subspaces.

\textbf{H2 confirmed for $\pi$ vs.\ $\eta_k$.}
The projection tests are decisive: removing the $\pi$ subspace from activations
causes $R^2$ drops of less than 0.001 for all $\eta_k$ targets, and removing
any $\eta_k$ subspace leaves $\pi$ regression unchanged.
The $\pi$--$\eta_k$ subspace overlaps (0.17--0.19) confirm near-orthogonality.
The transformer has cleanly separated meta-belief from within-component belief
into distinct residual stream directions.

\textbf{H2 not confirmed for $\eta_k$ vs.\ $\eta_{k'}$.}
H2 predicts four mutually orthogonal subspaces, but the three $\eta_k$
subspaces overlap substantially with each other (0.53--0.69).
This is more consistent with H3 (Superposition): the model does not dedicate a
separate 2D subspace to each component's fractal, but instead packs the three
within-component beliefs into a shared, partially overlapping region of
residual stream space.
The vary-one analysis shows that component-specific fits are more distinct
(A--C overlap 0.208) than the global fits suggest, so the components are not
fully merged, but they are far from orthogonal.

The pre-registered prediction (CEV dimensionality $\approx 8$) was not confirmed:
both layers use only 5--6 dimensions to explain 95\% of variance, consistent with
the compressed $\eta_k$ representation described above.

A notable surprise in the training dynamics: within-component beliefs ($\eta_k$)
emerge early (step~100, $R^2 > 0.9$), while the meta-belief ($\pi$) lags behind
(step~2{,}000).
This suggests the model first learns to track per-component token statistics
(which reduces loss immediately) and only subsequently learns to distinguish
\emph{which} component is active (which requires cross-component comparison).

\subsection{Implications}

\textbf{Non-ergodic pretraining induces partial factoring.}
The transformer cleanly separates ``what component am I in?'' ($\pi$) from
``where within this component am I?'' ($\eta$) without explicit supervision.
However, the three within-component beliefs are not further separated from each
other; they share residual stream directions, suggesting that factoring happens
at the coarsest level of the hierarchy (meta vs.\ within) but not below it.

\textbf{Two-level belief structure maps onto two-phase training dynamics.}
Within-component geometry is learned first and meta-belief routing emerges
second; the ordering reflects loss reduction efficiency, not conceptual
priority.

\textbf{In-context learning has geometric structure.}
ICL in real language models may involve a $\pi$-like routing circuit that
activates domain-specific $\eta$-like representations.
The factored geometry found here suggests these circuits can be linearly
separated in residual stream space.

\subsection{Limitations}

The model is small ($d=64$) with a short context window ($L=16$), and whether
these results generalise to larger architectures remains an open question.
The symmetric 3-state/3-token structure of Mess3 makes components easier to
distinguish than typical natural language domains, which may make the factored
geometry easier to learn than in practice.
Our ground-truth belief computation relies on exact analytical updates that are
not available for real corpora, so the regression analysis cannot be applied
directly to language models without synthetic proxy tasks.

% ──────────────────────────────────────────────────────────────────────────────
\section{Conclusion}
\label{sec:conclusion}
% ──────────────────────────────────────────────────────────────────────────────

A transformer trained on a non-ergodic mixture of Mess3 processes learns a
hierarchically factored residual stream geometry: meta-belief $\pi$ is encoded
in a subspace nearly orthogonal to all within-component beliefs $\eta_k$, while
the $\eta_k$ subspaces are partially entangled due to their shared geometric
structure on the 2-simplex.
$R^2(\pi)$ rises sharply by position~2, earlier than the synchronisation
horizon $N^* \approx 9.6$ predicted by Riechers et al.~\cite{riechers2025}:
linear decodability requires only systematic covariation with component
identity, which the first highly-diagnostic token already provides, while
full synchronisation (commitment to one vertex) still requires $\approx N^*$
tokens.
Training dynamics show $\eta_k$ representations emerge an order of magnitude earlier than $\pi$ (step~100 vs.\ 2{,}000), reflecting loss-gradient priority.
A dimension sweep confirms this hierarchy persists under capacity pressure: the model sacrifices $\pi$ encoding before $\eta_k$ encoding.
Future work should test whether these geometric signatures generalise to larger
models and richer token alphabets.

% ──────────────────────────────────────────────────────────────────────────────
\FloatBarrier
\begin{thebibliography}{9}

\bibitem{shai2024belief}
A.~Shai, S.~Doshi, F.~Langford, and C.~J. Hillar.
\newblock Transformers represent belief state geometry in their residual stream.
\newblock In \emph{Advances in Neural Information Processing Systems (NeurIPS)}, 2024.

\bibitem{riechers2025}
P.~Riechers, T.~McGrath, and A.~Tegmark.
\newblock Next-token pretraining implies in-context learning.
\newblock arXiv:2505.18373, 2025.

\bibitem{shai2025factored}
A.~Shai, S.~Doshi, F.~Langford, and C.~J. Hillar.
\newblock Transformers learn factored representations.
\newblock In \emph{International Conference on Machine Learning (ICML)}, 2025.

\bibitem{nanda2022tl}
N.~Nanda and T.~Lieberum.
\newblock {TransformerLens}: A library for mechanistic interpretability of
  {GPT}-2-style language models.
\newblock \url{https://github.com/neelnanda-io/TransformerLens}, 2022.

\bibitem{elhage2022superposition}
N.~Elhage et al.
\newblock Toy models of superposition.
\newblock \emph{Transformer Circuits Thread}, 2022.
\newblock \url{https://transformer-circuits.pub/2022/toy_model/index.html}.

\bibitem{olsson2022induction}
C.~Olsson et al.
\newblock In-context learning and induction heads.
\newblock \emph{Transformer Circuits Thread}, 2022.
\newblock \url{https://transformer-circuits.pub/2022/in-context-learning-and-induction-heads/index.html}.

\bibitem{li2023othello}
K.~Li et al.
\newblock Emergent world representations: Exploring a sequence model trained on a synthetic task.
\newblock In \emph{ICLR}, 2023. arXiv:2210.13382.

\bibitem{saxe2019semantic}
A.~M.~Saxe, J.~L.~McClelland, and S.~Ganguli.
\newblock A mathematical theory of semantic development in deep neural networks.
\newblock \emph{PNAS}, 116(23):11537--11546, 2019.

\end{thebibliography}

\end{document}
